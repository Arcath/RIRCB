= RubyIRCBot

RIRCB is an IRC Bot written in Ruby (Im very good at names)

= Usage

First you need to run the setup rake task:

    rake setup
    
At the moment script/run will only lurk on a channel
